import 'package:flutter/material.dart';

class RebatesScreen extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Rebates')),
      body: Center(child: Text('Rebates Placeholder')),
    );
  }
}